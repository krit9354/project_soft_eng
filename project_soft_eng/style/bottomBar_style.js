import { StyleSheet } from "react-native";

export const myStyle = StyleSheet.create({
    bottom_bar_box : {
        width : '100%',
        height : 100,
        height: 60, // ความสูงของ bottom bar
        backgroundColor: '#ddd', // สีพื้นหลัง
        justifyContent: 'center', // จัดข้อความให้อยู่ตรงกลางในแนวตั้ง
        alignItems: 'center', 
    }
})